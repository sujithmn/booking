name: booking
description: TPC booking

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  cupertino_icons: ^1.0.2
  google_fonts: ^2.3.3
  flutter_toggle_tab: ^1.2.0
  flutter_spinbox: ^0.9.0
  url_launcher: ^6.0.20
  geocode: 1.0.2
  timelines: ^0.1.0
  flutter_bloc: ^8.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
  - images/